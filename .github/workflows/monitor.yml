name: 监控 α 头目刷新

on:
  schedule:
    - cron: '*/53 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: 配置 Git 用户
        run: |
          git config --global user.name "alert-bot"
          git config --global user.email "bot@example.com"

      # 使用 PAT 替代默认 token
      - name: 检出代码
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.REPO_PAT }}   # ✅ 使用有写权限的 PAT
          fetch-depth: 0

      - name: 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: |
          pip install selenium webdriver-manager requests

      - name: 运行检测脚本
        env:
          SENDKEY: ${{ secrets.SENDKEY }}
        run: python detect.py

      - name: 提交最新状态
        run: |
          git add last_three_sum.json
          if ! git diff --cached --quiet; then
            git commit -m "[auto] 更新头目状态 $(date)"
            git push origin main
            echo "✅ 状态已提交"
          else
            echo "🔍 无变化，跳过提交"
          fi
